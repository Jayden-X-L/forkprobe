{
  "name": "forkprobe-selection-telemetry",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply forkprobe-selection-telemetry --local",
    "db:migrate:remote": "wrangler d1 migrations apply forkprobe-selection-telemetry --remote"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
